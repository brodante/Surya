#include<bits/stdc++.h>
using namespace std;
int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int n;
  cin>>n;
  int temp=n;
  for(int i=n;i>0;i--)
    {
      for(int j=n;j>0;j--)
        {
          if(j-i==0)
            {
              cout<<j;
              temp=
            }
          else
            {
              cout<<temp;
            }
        }
      cout<<'\n';
    }

  for(int i=1;i<=n;i++)
    {

    }
  return 0;
}
